interface:
  display_name: "Apps in Toss Start"
  short_description: "Onboarding and policy basics for Apps in Toss"
  default_prompt: "Use $ait-start to check whether this Apps in Toss service idea can be registered and what console/onboarding steps are needed."
policy:
  allow_implicit_invocation: true
